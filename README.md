# JPG/PNG to PDF Converter (Node.js + Express)

This project allows users to convert PNG or JPG images into downloadable PDF files using the `pdf-lib` library.

## Features
- Upload an image or use a predefined one
- Generates a properly formatted A4-sized PDF
- Built using Express.js and pdf-lib

## Usage
Start server:
```bash
node backend/server.js
